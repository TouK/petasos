apiVersion: v1
kind: Service
metadata:
  name: {{ include "petasos.fullname" . }}
  labels:
    {{- include "petasos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "petasos.selectorLabels" . | nindent 4 }}
